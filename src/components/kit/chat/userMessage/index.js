import {
    StyledUserMessage,
} from './userMessage.styles';

function UserMessage(props) {
    return (
        <StyledUserMessage>
hey
        </StyledUserMessage>
    );
}

export default UserMessage;
