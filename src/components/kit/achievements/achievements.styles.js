import styled from 'styled-components';

const StyledAchievements = styled.div`
  display: flex;
  align-items: flex-end;
  svg {
    margin: 0.4rem;
    pointer-events: none;
  }
`;

export { StyledAchievements };
